import { EventEmitter } from '@angular/core';
import { SizerComponent } from './sizer.component';

fdescribe('SizerComponent', () => {
  let size: number;
  let component = new SizerComponent();

  beforeAll(() => {
    component = new SizerComponent();
  });

  describe('dec', () => {

  });

  describe('inc', () => {

  });
});
